interface:
  display_name: "Paper Figure Splitter"
  short_description: "Extract and split publication figures cleanly"
  default_prompt: "Use $paper-figure-splitter to extract every figure from this paper and split composite figures into clean panels."
